# To run the program, put the code in `hello.py` and
# use `python`.
$ python hello.py
hello world

# Sometimes we'll want to run the program with arguments.
$ python hello.py --verbose
hello world
Verbose mode: enabled 
